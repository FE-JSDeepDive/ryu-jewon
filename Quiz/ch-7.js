/** 연산자를 활용한 반전 기법 예시 */
/** ---------------------------------------------------------------- */ 

// function extractRGB(color) {
//   const red = (color >> 5) & 0b111; 
//   const green = (color >> 2) & 0b111; 
//   const blue = color & 0b111; 
//   return { red, green, blue }; 
// }

// function invertColor(color) {
//   const { red, green, blue } = extractRGB(color);
  
//   const invertedRed = 7 - red;      
//   const invertedGreen = 7 - green;
//   const invertedBlue = 7 - blue;

//   const invertedColor = (invertedRed << 5) | (invertedGreen << 2) | invertedBlue;
//   return invertedColor;
// }

